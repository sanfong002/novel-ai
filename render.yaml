services:
  - type: web
    name: romance-chatbot
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false   # กรอกใน Render dashboard
      - key: NODE_ENV
        value: production
    healthCheckPath: /health